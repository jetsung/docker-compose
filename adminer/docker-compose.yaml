version: '3'

services:

  adminer:
    image: adminer
    container_name: adminer
    restart: unless-stopped
    ports:
      - 8080:8080
    networks:
      - db
      
networks:
  db:
    name: mariadb
    external: false

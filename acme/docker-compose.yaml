version: "3"
services:
  acme:
    image: neilpang/acme.sh
    container_name: acme.sh
    environment:
      - Ali_Key=${Ali_Key}
      - Ali_Secret=${Ali_Secret}
    volumes:
      - ./out:/acme.sh
      - ./ssl:/data/ssl
    restart: unless-stopped
    command: ["daemon"]


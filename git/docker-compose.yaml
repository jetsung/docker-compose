version: '3'
services:
  git:
    image: devcto/git:latest
    container_name: git-alpine
    restart: unless-stopped
    volumes:
      - ./init.sh:/app/init.sh
      - ./.ssh:/root/.ssh
      - /work_code:/srv
    command: -D
    